# Draft log

One row per draft the OS produced, and what happened to it (sent as-is, edited, ignored, superseded). The evening run reads this to close the voice loop. Keep the last ~14 days; older rows rotate to the archive.
